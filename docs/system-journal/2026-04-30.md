# System Journal 2026-04-30

- Gerado em: `2026-04-30 11:36:00` (Europe/Lisbon)
- Fonte principal de estado: `/tmp/state.json`

## Situação Do Sistema

- Trades reais fechadas no dia: `1`
- Wins / Losses / Flats: `0` / `1` / `0`
- PnL médio por trade do dia: `-0.724%`
- PnL realizado conhecido do dia: `-0.289 USDC` em `1` trade(s) com USD registado
- Posições reais abertas no snapshot: `1`

## Trades Reais Do Dia

- `LINKUSDC 5m cipherContinuationLong` -> `SL` | `-0.724%` | `-0.289 USDC` | fechada às `2026-04-30 03:30:47`

## Posições Abertas No Snapshot

- `BTCUSDC 1h cipherContinuationShort SHORT` | entry `75651.6` | barsOpen `7` | BE trigger 0.30R / lock 0.10R

## Modificações Do Dia

- 27f5790 2026-04-30 Add BTC-specific break-even overrides
- 4c54e60 2026-04-30 Guard real trades against disabled protections
- c5f0331 2026-04-30 Relax Twelve Data pacing for richer tradfi research
- d3473fb 2026-04-30 Add Binance public history backfill for long crypto datasets
- 8dfdca4 2026-04-30 Tune LINK SR hard block to resistance only

## Avarias E Observações Manuais

- Incidente principal do dia: uma trade real de `LINKUSDC 5m cipherContinuationLong` fechou em loss enquanto as proteções locais estavam efetivamente desligadas no live por configuração incompleta do `.env`.
- Correção aplicada no próprio dia:
  - `FUTURES_ATTACH_TPSL_ON_ENTRY` mantido em `0` porque o attach inicial ainda não é fiável.
  - `BREAK_EVEN_ENABLED=1` reativado no servidor.
  - `BREAK_EVEN_TRIGGER_R=0.40`, `BREAK_EVEN_LOCK_R=0.02`, `BREAK_EVEN_MIN_BARS=1` repostos no `.env`.
- Hardening adicional aplicado:
  - o executor agora recusa abrir trades reais se `attach TP/SL` e `break-even` estiverem ambos desligados ao mesmo tempo.
- Ajuste específico adicional:
  - `BTCUSDC 1h cipherContinuationShort` recebeu override de gestão `BE 0.30 / lock 0.10 / minBars 1` com base em sweep de replay com custos.
- Observação aberta:
  - a posição `BTCUSDC` que já estava aberta antes da correção não recupera retroativamente o trigger perdido; o objetivo da mudança é evitar repetição do problema e melhorar a gestão daqui para a frente.
- Revisão com histórico longo:
  - `BTCUSDC 1h breakdownRetestShort` e `cipherContinuationShort` degradaram fortemente quando saímos da amostra curta; perderam licença para core.
  - `ETHUSDC 1h cipherContinuationShort` também ficou negativo na janela longa.
  - `ADAUSDC 5m cipherContinuationLong` continuou negativo mesmo sem `preMacdStructureOverride`; o override piora, mas não é o único problema.
  - `LINKUSDC 5m cipherContinuationLong` continua vivo, desde que o hard block fique só em `resistance_too_close`.
  - `1000SHIBUSDC 15m cipherContinuationShort` continua a ser o lane mais limpo do core atual em janela longa.

## Sinais De Obstáculo

- Houve 1 loss(es) real(is) no dia; é preciso validar se foram mismatch de regime, chase ou gestão tardia.
- O core atual já mostrou 2 caso(s) não-TP que chegaram a pelo menos 80% do caminho até ao alvo; isso aponta para gestão/TP a precisar de calibração fina, sobretudo quando o movimento “quase entrega”.
- Existem 1 posição(ões) real(is) aberta(s), por isso o estado do sistema ainda está parcialmente em risco intradiário.
- O strategy hunt não está a devolver candidatos fortes neste snapshot; o lab continua mais útil como triagem do que como fonte imediata de promoção.

## Near-TP / Reversões Do Core

- Trades fechadas do core com dados suficientes: `11`
- Não-TP do core que chegaram a pelo menos 80% do caminho até ao alvo: `2`
- Não-TP do core que chegaram a pelo menos 90% do caminho até ao alvo: `1`
- Lanes com mais fricção:
  - `ADAUSDC|5m|cipherContinuationLong`: non-TP `2`, near80 `2`, near90 `1`, avgNonTP `+0.030%`
  - `ETHUSDC|1h|cipherContinuationShort`: non-TP `1`, near80 `0`, near90 `0`, avgNonTP `-0.961%`
  - `LINKUSDC|5m|cipherContinuationLong`: non-TP `1`, near80 `0`, near90 `0`, avgNonTP `-0.724%`

## Snapshot Do Lab / Hunt

- counts: live `0`, observe `0`, archive `0`
- Sem candidatos destacados no snapshot atual.

## Plano De Mitigação

- Monitorizar o lane `BTCUSDC 1h short` com a gestão específica nova (`BE 0.30 / lock 0.10`) até termos amostra suficiente para confirmar se reduz o dano sem matar edge.
- Medir no lab se `ADAUSDC 5m cipherContinuationLong` beneficia mais de `front-run` ligeiro do TP ou de break-even/lock mais cedo antes de zonas óbvias de liquidez.
- Continuar a reforçar cobertura bearish/líder no live e no lab, em vez de afrouxar `ADA/LINK long` às cegas.
- Manter o hunt a rodar e melhorar o registry para distinguir melhor `observe promissor` de `arquivo sem edge`, evitando desperdiçar tempo com famílias já mortas.
- Aplicar no live um `de-risking` estrutural sem restart enquanto a `BTC` aberta continua viva:
  - desligar `ADAUSDC 5m cipherContinuationLong`
  - desligar `ETHUSDC 1h cipherContinuationShort`
  - desligar `BTCUSDC 1h breakdownRetestShort` e `cipherContinuationShort`
  - manter `BTCUSDC 1h cipherContinuationLong`, `LINKUSDC 5m cipherContinuationLong` e `1000SHIBUSDC 15m cipherContinuationShort`
- Edge hunt posterior com histórico longo:
  - `1000SHIBUSDC 15m cipherContinuationShort` melhorou ao endurecer qualidade.
  - melhor variante testada:
    - `minScore = 96`
    - `minAdx = 12`
    - `41` trades
    - `avgNet +0.2051%`
    - `PF 1.577`
    - `maxDD 4.1182%`
- `1000PEPEUSDC 1h cipherContinuationLong` ficou como candidato de `observe/lab`, não de promoção imediata:
  - `21` trades
  - `avgNet +0.3012%`
  - `PF 1.219`
  - `maxDD 9.5556%`
- Expansão controlada do lab para aumentar cobertura sem baixar padrões:
  - o `narrow hunt` passou a usar rotação de símbolos em `3` blocos, cobrindo `12` tokens em `3` rondas horárias sem aumentar o tamanho de cada sweep
  - o `manual/default hunt` foi alinhado com o universo largo do `broad hunt`
  - os gates de classificação (`live / observe / archive`) ficaram intactos; aumentámos cobertura, não permissividade
